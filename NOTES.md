# 教学笔记

- 2026-09-07：用户选择亲手实现简化 Transformer、弄懂内部工作机制作为主线；微调作为后续目标保留。
- 用户自述理解变量、乘法、`abs()` 与 `print()`；列表、循环、条件判断、自定义函数及向量、矩阵、导数尚未确认。按需解释。
- 2026-09-07：用户指出直接讲参数可能过快，并批准从机器学习的用途开始调整。避免把“计算简单”误当成“概念容易”。
- 当前实验为 `exercises/0002-gradient-descent.py`。用户已授权 uv 初始化，默认 Python 3.12，无第三方依赖，后续通过 `uv run` 运行。
- 当前对话已从绝对误差推进到单例平方误差及其梯度；不要将其与旧草稿的平均平方误差混淆。
- 路线与当前状态见 `PROGRESS.md`。原参数实验移至 `drafts/model-parameters.html`，待拆成参数与损失两次教学。

- 2026-09-08：已运行 `exercises/0003-batch-gradient-descent.py`。三个训练例子共享 w，整轮累计后更新；输入 4 只在训练结束后预测，标签 8 仅用于核对。当前损失为多个例子的均方误差。

- 2026-09-08：用户明确要求加快节奏，用小型 demo 集中教学，先运行、看结果、修改条件，再补概念。已批准并完成 PyTorch 噪声直线 demo。不要逐个概念反复确认。
- 当前 WSL 可见 8 逻辑 CPU、约 11 GiB 内存，RTX 4060 Laptop 8 GB；PyTorch 2.14.0 CUDA 13.0 已实测 GPU 矩阵运算和反向传播。首个 demo 使用 CPU。资源空闲量会变化。

- 2026-09-08：用户批准进入 Demo 2。已提供二维圆分类，先解释 Linear(2,16) → Tanh → Linear(16,1) 和形状，再按疑问补张量或交叉熵知识。不要假设用户理解新 API。
